<div
	class="fixed top-0 flex h-[74px] w-full max-w-[1440px] items-center justify-between bg-[white] px-3 py-5 drop-shadow-lg"
>
	<!-- Burger -->
	<input type="checkbox" id="burger-toggle" class="hidden" />
	<label
		for="burger-toggle"
		class="burger flex h-fit w-20 cursor-pointer flex-col justify-start gap-1 lg:hidden"
	>
		<span class="block h-1 w-8 rounded-md bg-blue"></span>
		<span class="block h-1 w-8 rounded-md bg-blue"></span>
		<span class="block h-1 w-8 rounded-md bg-blue"></span>
	</label>

	<!-- Logo -->
	<a href="#">
		<img src="/src/resources/logo.svg" alt="Logo" class="h-auto w-36" />
	</a>

	<!-- Language -->
	<div
		class="languageBox flex h-fit w-20 items-center justify-end gap-3 text-blue lg:order-3 lg:w-36"
	>
		<button>Es</button>
		<div class="block h-5 w-0.5 rounded-md bg-black"></div>
		<button>En</button>
	</div>

	<!-- Menu -->
	<div
		id="menu"
		class="menu lg:bg-transparent absolute left-0 top-[74px] flex h-[calc(100dvh-74px)] w-full flex-col gap-8 bg-white p-5 lg:relative lg:top-0 lg:order-2 lg:col-start-2 lg:h-auto lg:w-auto lg:flex-row lg:gap-4"
	>
		<a href="#" class="flex justify-end lg:hidden">
			<svg
				id="closeNav"
				width="14"
				height="14"
				viewBox="0 0 14 14"
				fill="none"
				xmlns="http://www.w3.org/2000/svg"
			>
				<path
					d="M14 1.41L12.59 0L7 5.59L1.41 0L0 1.41L5.59 7L0 12.59L1.41 14L7 8.41L12.59 14L14 12.59L8.41 7L14 1.41Z"
					fill="#141518"></path>
			</svg>
		</a>
		<a href="#inicio" class="">Inicio</a>
		<a href="#nosotros" class="">Sobre Nosotros</a>
		<a href="#servicios" class="">Servicios</a>
		<a href="#contacto" class="">Contacto</a>
		<a href="#clientes" class="">Nuestros Clientes</a>
		<a href="#trabaja" class="block">Trabaja con nosotros</a>
	</div>
</div>

<script>
	document.getElementById('closeNav').addEventListener('click', function () {
		document.getElementById('burger-toggle').checked = false
	})
	document.querySelectorAll('a').forEach(function (element) {
		element.addEventListener('click', function () {
			document.getElementById('burger-toggle').checked = false
		})
	})
</script>

<style>
	#burger-toggle:checked ~ #menu {
		display: flex;
	}

	#burger-toggle:not(:checked) ~ #menu {
		display: none;
	}

	@media screen and (min-width: 1024px) {
		#burger-toggle:not(:checked) ~ #menu {
			display: flex;
		}
	}

	.languageBox,
	a {
		font-family: 'Roboto', sans-serif;
		font-weight: 400;
	}
</style>
